---
title: "OpenAI STT pricing"
sidebarTitle: "OpenAI"
description: "7 STT model(s) from OpenAI, with the rate and where it came from."
mode: "wide"
---

{/* Generated by `make build-docs` from prices/data.json. Do not edit by hand. */}

Priced from `input_audio_kseconds`, US dollars per 1,000 seconds of input audio, and shown converted to a per-minute rate for comparison. Vendors quote in whatever unit they prefer (per minute, per second, per hour), so the figure here is a conversion, not a quote. Source of truth: [`prices/providers/openai.yml`](https://github.com/mahimailabs/voice-prices/blob/main/prices/providers/openai.yml).

**Plan:** Standard. Rates below are that plan. A committed or negotiated plan will be cheaper, so check yours before budgeting from these.

| Model | Name | $ / min | Source | Last verified |
| --- | --- | ---: | ---: | ---: |
| `gpt-4o-mini-transcribe` <sup>estimated</sup> | GPT-4o mini Transcribe | $0.003 | <Badge color="green" size="sm">verified</Badge> | 2026-08-20 |
| `gpt-4o-transcribe` <sup>estimated</sup> | GPT-4o Transcribe | $0.006 | <Badge color="green" size="sm">verified</Badge> | 2026-08-20 |
| `gpt-live-transcribe` | GPT Live Transcribe | $0.01699998 | <Badge color="green" size="sm">verified</Badge> | 2026-08-20 |
| `gpt-realtime-translate` | GPT-Realtime-Translate | $0.03400002 | <Badge color="green" size="sm">verified</Badge> | 2026-08-20 |
| `gpt-realtime-whisper` | GPT-Realtime-Whisper | $0.01699998 | <Badge color="green" size="sm">verified</Badge> | 2026-08-20 |
| `gpt-transcribe` | GPT Transcribe | $0.0045 | <Badge color="green" size="sm">verified</Badge> | 2026-08-20 |
| `whisper-1` | Whisper | $0.006 | <Badge color="green" size="sm">verified</Badge> | 2026-05-29 |

- `estimated` this rate is the vendor's own published estimate, not the meter it bills on. It will not reconcile against an invoice: the model is billed in another unit (usually tokens) and the figure here is the vendor restating that in this one. Useful for comparison and capacity planning, not for billing.

What `verified`, `imported`, and `seed` mean is on [How Fresh?](/how-fresh). A rate that looks wrong is worth [reporting](https://github.com/mahimailabs/voice-prices/issues/new?template=add-provider.yml).
